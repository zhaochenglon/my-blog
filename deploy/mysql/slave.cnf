[mysqld]
server-id=2
# read_only / super_read_only 不可写在此：首次初始化时 entrypoint 需 SET PASSWORD，
# super_read_only 会导致 ERROR 1290，root 密码无法写入。复制就绪后由 setup-replication.sh 开启。
relay_log_recovery=ON
gtid_mode=ON
enforce_gtid_consistency=ON
character-set-server=utf8mb4
collation-server=utf8mb4_unicode_ci
