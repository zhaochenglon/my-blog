# ASP.NET 工程师进阶学习路径

> 面向 C# / ASP.NET Core 开发工程师，基于当前墨言博客项目（单体 API + Docker + Nginx + MySQL）整理。  
> 相关文档：[TECH.md](./TECH.md)（架构概念）、[README.md](./README.md)（部署实践）

---

## 一、技术全景与依赖关系

先建立「谁依赖谁」的全局图，再按阶段学习，避免一上来就啃微服务/K8s 却连幂等和缓存都没落地。

```mermaid
flowchart TB
  subgraph 基础层
    IOC[IOC / 依赖注入]
    AOP[AOP / 中间件]
    DDD[DDD + 仓储模式]
  end

  subgraph 单机扩展层
    NG[Nginx 反代 / 负载均衡]
    RD[Redis]
    DBMS[主从数据库 / 读写分离]
  end

  subgraph 分布式能力层
    HA[高并发 / 高可用 / 幂等]
    MQ[RabbitMQ]
    ES[ElasticSearch]
  end

  subgraph 工程与架构层
    DK[Docker / K8s]
    CICD[CI/CD DevOps]
    MS[微服务架构思想]
  end

  IOC --> DDD
  AOP --> DDD
  DDD --> RD
  NG --> HA
  RD --> HA
  DBMS --> HA
  HA --> MQ
  MQ --> ES
  DK --> CICD
  CICD --> MS
  HA --> MS
  MQ --> MS
  ES --> MS
  DK --> MS
```

**核心原则：** 先能在单体里把事做对（IOC、仓储、Redis、幂等），再学怎么拆（微服务）和怎么运（K8s、CI/CD）。

---

## 二、推荐学习顺序（6 个阶段）

| 阶段 | 周期建议 | 主题 | 为什么在这个位置 |
|------|----------|------|------------------|
| **0** | 已完成 ✅ | Docker 基础、Nginx 反代、单体部署 | 你的博客项目已实践，作为后续所有实验的「靶场」 |
| **1** | 3～4 周 | IOC / AOP、DDD + 仓储模式 | 代码组织与架构思维的地基；ASP.NET Core 每天都在用，但要「知其所以然」 |
| **2** | 4～6 周 | Nginx 负载均衡、Redis、主从数据库 | 单机 → 多实例 → 读写分离，解决「流量变大、读多写少」 |
| **3** | 4～6 周 | 高并发 / 高可用 / 幂等 | 在 Redis + 数据库基础上，处理重复请求、并发写、故障恢复 |
| **4** | 4～6 周 | RabbitMQ、ElasticSearch | 异步解耦 + 搜索；与幂等、最终一致强相关 |
| **5** | 6～8 周 | CI/CD、Docker 进阶、K8s | 把上面能力自动化交付、集群化运行 |
| **6** | 持续 | 微服务架构设计思想 | **最后学**：需要前面所有组件都「用过、踩过坑」再谈拆分 |

**总时长：** 约 6～12 个月（在职学习，按每周 8～12 小时估算）。

---

## 三、分阶段详解

### 阶段 0：已有基础（复盘即可）

你已在 [README.md](./README.md) 中完成：

- Docker Compose 跑 MySQL + `blog-api`
- Nginx 反向代理 `/api/` → 内网 5000 端口
- 云服务器部署与 Git 更新

**复盘任务：** 能不看文档，口述一次「用户提交留言 → Nginx → API → EF Core → MySQL」全链路。

---

### 阶段 1：IOC / AOP + DDD + 仓储模式

> **目标：** 写出结构清晰、可测试、易扩展的单体代码——这也是微服务里「每个服务内部」应有的样子。

#### 1.1 IOC（控制反转）与依赖注入

使用AUTOFAC框架，创建IDependency空接口，每个接口都会继承这个空接口，然后在AutofacModule.cs中,使用autofac将所有IService层自动注入,在使用的时候使用构造函数直接注入

Controller → IService → IRepository → Dapper/SQL
                ↑              ↑
           Autofac 容器按接口自动注入


| 要点 | 说明 |
|------|------|
| 是什么 | 对象依赖由容器创建和注入，而不是 `new` |
| ASP.NET 中的体现 | `Program.cs` 里 `builder.Services.AddScoped<IMessageRepository, MessageRepository>()` |
| 生命周期 | Singleton / Scoped / Transient 的区别与误用场景 |
| 与单元测试 | 接口 + Mock，可替换真实数据库 |

**实践建议（博客项目）：**

1. 把 `MessagesController` 里直接访问 `DbContext` 的逻辑，抽到 `IMessageRepository` + `MessageRepository`。
2. 把「校验 + 写库」抽到 `IMessageService`，Controller 只负责 HTTP。

**达标标准：** 能解释为什么 Controller 不应直接 `new DbContext`，以及 Scoped 为何适合 Repository。

#### 1.2 AOP（面向切面编程）

| 要点 | 说明 |
|------|------|
| 是什么 | 日志、鉴权、异常、缓存等横切关注点与业务代码分离 |
| ASP.NET 中的体现 | **中间件（Middleware）**、**Filter**、**Interceptor**（EF / Castle DynamicProxy） |
| 常见场景 | 统一异常处理、请求日志、性能计时、权限校验 |

**你已在 CRUD 项目中实践 ✅**（[asp.net-core-web-api-curd](https://github.com/zhaochenglon/asp.net-core-web-api-curd)）：

而是把「记录谁、调了哪个接口、传了什么、返回了什么」从 Controller/Service 里完全剥离出来，业务代码不用写一行日志代码。

- `RequestLoggingMiddleware`：基于 HTTP 管道的请求/响应日志切面
- 跳过 GET、`/health`、`/swagger` 等噪声请求
- `EnableBuffering()` 读请求体后重置流，不影响后续 Controller
- `ResponseCaptureStream` 包装响应流，同时写回客户端并捕获内容
- `finally` 里 `_ = LogAsync(...)` 异步写库，不阻塞 HTTP 响应
- 另有 `DynamicAuthorizeFilter` 做权限验证（Filter 型 AOP）

```
请求 → RequestLoggingMiddleware → 路由 → Filter → Controller → 响应
              ↑ 横切：日志、鉴权，业务代码无感知
```

**博客项目待补：**

1. 迁移 `RequestLoggingMiddleware`（留言 POST/DELETE 即可，GET 列表可跳过）
2. 补一个**全局异常处理中间件**，统一 JSON 错误格式（CRUD 项目 catch 后 rethrow，可再完善）
3. 了解 `IHostedService` 做后台任务（与 MQ 消费预热）

**达标标准：** 能区分 Middleware vs Filter 的执行顺序和适用场景；能解释为何日志放 Middleware、权限放 Filter。

> 管道顺序备忘：Authentication/Authorization 中间件早于 Logging 中间件（日志才能读到 `HttpContext.User`）；`DynamicAuthorizeFilter` 在 `MapControllers` 之后的 MVC 阶段执行。

#### 1.3 DDD（领域驱动设计）+ 仓储模式

| 要点 | 说明 |
|------|------|
| DDD 核心 | 以业务领域为中心：实体、值对象、聚合根、领域服务、领域事件 |
| 仓储模式 | 持久化细节对领域层透明；`IRepository<T>` 抽象数据访问 |
| 不必一步到位 | 博客规模用「轻量 DDD」即可：Service + Repository + 清晰命名 |
| 与 EF Core | Repository 内部用 DbContext；复杂查询可用 Specification 模式 |

**你已在 CRUD 项目中实践 ✅**（[asp.net-core-web-api-curd](https://github.com/zhaochenglon/asp.net-core-web-api-curd)）：

```
Controller → IService → IRepository → DapperRepository<T> → MySQL
                              ↑
                    IDapperRepository<T> 泛型 CRUD（Insert/Get/Update/Delete/分页/事务）
```

- **三层分工**：Controller 只调 Service；Service 写业务（如 `FileService.saveAttachment`）；Repository 只管持久化
- **泛型仓储基类**：`DapperRepository<TEntity>` 封装 DapperExtensions 通用方法 + `RunInTransaction`
- **实体专属接口**：`Isys_fileRepository : IDapperRepository<sys_file>`，实现类 `sys_fileRepository` 继承基类即可
- **Autofac 批量注册**：Repository/Service 均实现 `IDependency`，自动注入

**架构定位（面试可说清）：** 这是 **「三层架构 + 泛型仓储（Generic Repository）」**，已具备仓储模式的工程价值；与「经典 DDD」的差距主要在贫血模型、仓储接口偏通用——对 CRUD 型系统完全够用。

**博客项目待补：**

1. 补齐 Controller → Service → Repository 三层（当前 Controller 直接注入 `AppDbContext`）
2. 博客可用 **专用仓储接口**（`IMessageRepository` 只暴露 `AddAsync`/`ListAsync`），不必照搬泛型 CRUD 全暴露给 Service
3. 给 `Message` 加简单领域规则（创建时校验 Email、内容长度）

**达标标准：** 能画出分层图；能说明「泛型仓储 vs 按聚合根定制仓储」的区别；能解释你的 demo 属于哪一类。

---

### 阶段 2：Nginx 负载均衡 + Redis + 主从数据库

> **目标：** 理解「应用层横向扩展」和「数据层读扩展」，为高并发打基础。

#### 2.1 Nginx 反向代理与负载均衡

| 主题 | 内容 |
|------|------|
| 反向代理 | 你已在用：`proxy_pass` 把 `/api/` 转到后端 |
| 负载均衡 | `upstream` 配置多台相同 API 实例，轮询 / 加权 / `ip_hash` |
| 健康检查 | `max_fails`、`fail_timeout`；生产可用云 SLB |
| HTTPS | `certbot` 或云证书，与 `FRONTEND_ORIGIN` 联动 |

**实践建议：**

1. 本地用两个端口启动两个 `blog-api` 实例，Nginx `upstream` 轮询。
2. 观察某实例挂掉时 Nginx 行为（理解高可用的入口层）。

**达标标准：** 能手写带 `upstream` 的 Nginx 配置，并解释与「微服务网关」的区别。

#### 2.2 Redis

| 主题 | 内容 |
|------|------|
| 数据结构 | String、Hash、List、Set、Sorted Set |
| 典型用途 | 缓存、Session、分布式锁、限流计数、排行榜 |
| .NET 客户端 | `StackExchange.Redis` 或 `Microsoft.Extensions.Caching.StackExchangeRedis` |
| 经典问题 | 缓存穿透 / 击穿 / 雪崩及对策 |

**实践建议（博客项目）：**

1. Docker Compose 增加 Redis 服务。✅ 见 `docker-compose.yml` / `docker-compose.prod.yml`
2. 为「留言列表」接口加缓存，TTL 10 分钟。✅ 见 `blog-api/Services/MessageListCacheService.cs`
3. 用 Redis 实现「同一 Email 60 秒内只能提交一次」限流。（待做）

**达标标准：** 能讲清 Cache-Aside 模式，以及更新数据库后如何失效缓存。

**本仓库实现要点：**

| 步骤 | 代码位置 |
|------|----------|
| 注册 Redis | `blog-api/Program.cs` → `AddStackExchangeRedisCache` |
| 读列表先查缓存 | `MessagesController.List` → `TryGetAsync` |
| 未命中写库并回填 | `SetAsync`，TTL 来自 `Redis:MessageListCacheMinutes` |
| 新增/删除后失效 | `InvalidateAsync` 递增 `messages:list:version` |

详细 Redis 特性与面试题见本文 **「八、附录：Redis 特性与面试题」**。

#### 2.3 主从数据库（Master-Slave）

| 主题 | 内容 |
|------|------|
| 原理 | 主库写 binlog，从库重放；读写分离 |
| 延迟 | 写完立刻读从库可能读到旧数据 → 关键读走主库或稍等 |
| 与并发 | 主从不替代行锁；并发写仍在主库 |
| 云实践 | 阿里云 RDS 高可用版、只读实例 |

**实践建议：**

1. 本地/服务器 Docker 主从：见 [DEPLOY-REPLICA.md](./DEPLOY-REPLICA.md)、`docker-compose.replica.yml`。
2. 代码已实现：`AppDbContext`（写主）+ `AppReadDbContext`（读从），`setup-replication.sh` 配置 GTID 复制。

**达标标准：** 能画「2 台 API + 1 主 1 从 MySQL」架构图，并说明各组件职责。

**推荐资料：**

- Redis 官方文档 Quick Start
- Nginx `ngx_http_upstream_module` 文档
- MySQL 8.0 Replication 文档

---

### 阶段 3：高并发 / 高可用 / 幂等

> **目标：** 在分布式、多实例、MQ 环境下，保证数据正确性和系统可恢复性。

#### 3.1 高并发

| 手段 | 说明 |
|------|------|
| 应用无状态 | Session 放 Redis 或 JWT；任意实例可处理请求 |
| 连接池 | 数据库、Redis、HttpClient 连接池调优 |
| 异步 | `async/await` 释放线程；避免阻塞 |
| 削峰 | 请求先入队（MQ），后台慢慢消费 |
| 限流 | Nginx `limit_req`、Redis 计数、网关限流 |

#### 3.2 高可用

| 层次 | 手段 |
|------|------|
| 入口 | Nginx / SLB 多实例 |
| 应用 | 多副本 + 健康检查 + 滚动发布 |
| 缓存 | Redis 哨兵 / 集群 |
| 数据库 | 主从 + 自动 failover（MHA / 云 RDS） |
| 降级熔断 | Polly（.NET 重试、熔断、超时） |

#### 3.3 幂等（极其重要）

| 场景 | 做法 |
|------|------|
| HTTP 重复提交 | 前端防抖 + 后端 `Idempotency-Key` + Redis/DB 去重 |
| 支付 / 回调 | 业务唯一键（订单号）+ 状态机，已处理则直接返回成功 |
| MQ 重复消费 | 消费前查 `messageId` 是否已处理；或数据库唯一约束 |
| 数据库 | 唯一索引（如 `Email + CreatedAt` 窗口）防止重复插入 |

**实践建议：**

1. 留言接口增加 `X-Request-Id`，Redis 记录 24h 内已处理的 ID。
2. 用 JMeter / k6 对留言接口压测，观察连接池与响应时间。
3. 引入 Polly 为调用外部 HTTP 接口加重试与熔断。

**达标标准：** 能举例说明「为什么 MQ 必须幂等」「为什么支付回调要幂等」，并在博客项目里实现一种去重方案。

**推荐资料：**

- 《高并发系统设计 40 问》（极客时间，选读）
- Polly 官方文档
- [TECH.md](./TECH.md) 第 2.5 节「并发与数据一致性」

---

### 阶段 4：RabbitMQ + ElasticSearch

> **目标：** 异步解耦与全文检索——微服务间协作的两块常见拼图。

#### 4.1 RabbitMQ

| 概念 | 说明 |
|------|------|
| 核心模型 | Exchange、Queue、Binding、Routing Key |
| 模式 | 简单队列、Work Queue、Pub/Sub、Routing、Topic |
| 可靠性 | 持久化、Publisher Confirm、Manual ACK、死信队列 |
| .NET | `RabbitMQ.Client` 或 MassTransit（更推荐，封装好） |

**实践建议：**

1. Docker 启动 RabbitMQ（带 Management 插件）。
2. 留言成功后发布 `MessageCreated` 事件 → 消费者发邮件通知 / 写日志（先 `Console.WriteLine` 即可）。
3. **重点：** 消费者用 `messageId` 做幂等，重复消息不重复发通知。

**达标标准：** 能画出 Producer → Exchange → Queue → Consumer 图，并解释 ACK 与死信队列用途。

#### 4.2 ElasticSearch

| 概念 | 说明 |
|------|------|
| 是什么 | 分布式搜索引擎，倒排索引，适合全文检索、日志分析 |
| 与 MySQL 关系 | MySQL 是真相源；ES 是查询视图，通过 MQ / Canal 同步 |
| 核心概念 | Index、Document、Mapping、Analyzer、Query DSL |
| .NET | NEST 或 Elastic.Clients.Elasticsearch |

**实践建议：**

1. Docker 启动单节点 ES + Kibana。
2. 将留言同步到 ES（可先同步调用，再用 RabbitMQ 异步同步）。
3. 实现「按关键词搜索留言内容」API，对比 MySQL `LIKE` 的性能差异。

**学习顺序说明：** 建议 **先 RabbitMQ 再 ES**，因为生产里常见「写 MySQL → 发 MQ → 消费者更新 ES」链路，幂等也在 MQ 侧练过一遍。

**达标标准：** 能解释「为什么搜索不用 MySQL LIKE 扛高并发」，以及 ES 与 MySQL 数据不一致时的处理思路。

**推荐资料：**

- RabbitMQ 官方 Get Started（含 .NET 示例）
- Elastic 官方 «Search Labs»
- MassTransit 文档（.NET 集成 RabbitMQ）

---

### 阶段 5：CI/CD + Docker 进阶 + Kubernetes

> **目标：** 代码提交后自动构建、测试、部署；容器从单机 Compose 走向集群编排。

#### 5.1 CI/CD（DevOps）

| 环节 | 典型工具 |
|------|----------|
| 代码托管 | GitHub / GitLab / Azure DevOps |
| CI | 拉代码 → `dotnet restore/build/test` → 构建 Docker 镜像 |
| CD | 推送镜像到仓库 → SSH / K8s 滚动更新 |
| 质量门禁 | 测试失败则阻断后续步骤（如 Docker 构建） |

**本项目 CI 已落地（2026-06）：** 编写自动化测试与流水线配置，推送到 GitHub 后自动执行——**先验证 API 接口与业务逻辑，再验证 Docker 镜像能否正常构建**。

##### 5.1.1 一句话理解

> 写好 `ci.yml`（流水线）和 `BlogApi.Tests`（测试用例），每次 `git push` 到 `main`（或向 `main` 提 PR）时，GitHub Actions 会在云端自动：**编译 → 跑 8 个测试 → 构建 Docker 镜像**。测试不过，Docker 那步不会执行。

##### 5.1.2 整体流程

```mermaid
flowchart TD
    A[git push / PR 到 main] --> B[Job 1: Build blog-api]
    B --> C[Checkout 拉代码]
    C --> D[Setup .NET 8]
    D --> E[Restore 下载 NuGet]
    E --> F[Build 编译]
    F --> G[Test 跑 8 个自动化测试]
    G --> H{测试通过?}
    H -->|否| I[CI 失败 ❌]
    H -->|是| J[Job 2: Build Docker image]
    J --> K[Checkout]
    K --> L[Build image 按 Dockerfile 打镜像]
    L --> M[CI 成功 ✅]
```

**两个 Job 的分工：**

| Job | 名称 | 做什么 | 验证什么 |
|-----|------|--------|----------|
| 1 | `build-api` | Restore → Build → **Test** | 代码能编译；**接口访问与鉴权逻辑**正确 |
| 2 | `build-docker` | `docker build`（`push: false`） | **Docker 镜像**能按 `Dockerfile` 正常打出（不推送、不部署） |

Job 2 通过 `needs: build-api` 依赖 Job 1：**先测接口，再测镜像**。

##### 5.1.3 相关文件清单

| 文件 | 作用 |
|------|------|
| [`.github/workflows/ci.yml`](./.github/workflows/ci.yml) | CI 流水线定义（触发条件、两个 Job、各 Step） |
| [`blog-api/BlogApi.Tests/`](./blog-api/BlogApi.Tests/) | xUnit 测试项目（单元测试 + 集成测试） |
| [`blog-api/BlogApi.Tests/BlogApiWebApplicationFactory.cs`](./blog-api/BlogApi.Tests/BlogApiWebApplicationFactory.cs) | 测试专用 API 宿主（InMemory 数据库，CI 无需 MySQL） |
| [`blog-api/Program.cs`](./blog-api/Program.cs) | `Testing` 环境跳过迁移；`partial class Program` 供集成测试引用 |
| [`blog-api/BlogApi.csproj`](./blog-api/BlogApi.csproj) | 排除 `BlogApi.Tests/**`，避免测试代码被主项目编译 |
| [`blog-api/Dockerfile`](./blog-api/Dockerfile) | Job 2 构建镜像的蓝图 |

##### 5.1.4 测试项目 `BlogApi.Tests`（共 8 个用例）

测试分两类：**单元测试**（直接测类）与**集成测试**（发真实 HTTP 请求测接口）。

| 文件 | 类型 | 测试内容 |
|------|------|----------|
| `AdminAuthServiceTests.cs` | 单元测试 | Token 生成与校验；ApiKey；密码校验 |
| `MessagesApiTests.cs` | 集成测试 | `POST /api/messages` 无需登录可留言；`GET /api/messages` 未鉴权 401；带 `X-Admin-Key` 可列表 |
| `AdminApiTests.cs` | 集成测试 | `POST /api/admin/login` 密码错误 401；正确凭据返回 Token |

**集成测试如何不依赖 MySQL：**

- `BlogApiWebApplicationFactory` 将 `AppDbContext` / `AppReadDbContext` 替换为 **EF Core InMemory**
- 环境变量设为 `Testing`，`Program.cs` 跳过 `db.Database.Migrate()`
- Redis 连接留空，自动 fallback 到内存缓存

##### 5.1.5 `ci.yml` 关键配置说明

```yaml
# 触发：push 或 PR 到 main
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Job 1：必须先恢复「测试项目」（会连带恢复主项目）
- run: dotnet restore BlogApi.Tests/BlogApi.Tests.csproj
- run: dotnet build BlogApi.Tests/BlogApi.Tests.csproj -c Release --no-restore
- run: dotnet test BlogApi.Tests/BlogApi.Tests.csproj -c Release --no-build

# Job 2：仅 build，不 push（CD 阶段再改为 push 到镜像仓库）
- uses: docker/build-push-action@v6
  with:
    context: ./blog-api
    push: false
    tags: blog-api:ci
```

> **踩坑记录：** `dotnet restore` 若只恢复主项目 `BlogApi.csproj`，下一步 `build BlogApi.Tests --no-restore` 会在 CI 报 `NETSDK1004`（找不到 `project.assets.json`）。应 **restore 测试项目 csproj**。

##### 5.1.6 本地与云端如何跑

**本地（与 CI 一致）：**

```powershell
cd blog-api
dotnet test BlogApi.Tests/BlogApi.Tests.csproj -c Release
```

**云端：** push 后到 GitHub 仓库 → **Actions** → 选择 **CI** 工作流查看各 Step 日志。

| Actions 中的 Step | 含义 |
|-------------------|------|
| Set up job | GitHub 分配 Ubuntu 虚拟机（自动） |
| Checkout | 克隆仓库代码 |
| Setup .NET 8 | 安装 .NET 8 SDK |
| Restore | 下载 NuGet 依赖 |
| Build | 编译 API + 测试项目 |
| Test | 执行 8 个自动化测试 |
| Build image | 按 `Dockerfile` 构建镜像（Job 2） |
| Post / Complete job | 收尾与销毁虚拟机（自动） |

##### 5.1.7 CI 与 CD、与服务器部署的区别

| | CI（已完成） | 服务器手动部署 | CD（待做） |
|---|-------------|----------------|------------|
| 编译 + 测试 | ✅ 自动 | 本地或服务器手动 | ✅ 自动 |
| Docker build | ✅ 自动验证 | `docker compose build` | ✅ 自动 |
| 推送镜像 | ❌ `push: false` | 可选 | push 到 ACR / Docker Hub |
| 启动服务 | ❌ | `docker compose up -d` | SSH 或 K8s 滚动更新 |
| MySQL / Redis | 测试用 InMemory | 真实容器 | 真实容器 |

**Build image 与服务器部署的关系：** 两边都用同一份 `blog-api/Dockerfile` 打 API 镜像；CI 只在云端试构建，不启动容器、不连数据库。服务器部署则是 build（或 pull）后 `compose up`，并注入 `.env` 中的连接串与密钥。

##### 5.1.8 后续 CD 实践建议（未做）

1. CI 中 `push: true`，将镜像推到阿里云 ACR 或 Docker Hub。
2. `main` 合并成功后，SSH 到云服务器执行 `docker compose pull && docker compose up -d`。
3. GitHub **Branch protection**：要求 CI 通过才能合并 `main`。

**达标标准（CI 部分）：** ✅ 一次 `git push` 能自动跑通 8 个测试；✅ 能说清 CI 与 CD 的区别；✅ 能解释「先测接口、再 build 镜像」的两段式 Job 设计。

#### 5.2 Docker 进阶

| 主题 | 内容 |
|------|------|
| 镜像优化 | 多阶段构建、`dotnet publish` 精简镜像体积 |
| 网络 | bridge / host；Compose 服务名 DNS |
| 数据卷 | 数据库持久化；备份策略 |
| 安全 | 非 root 用户运行容器；`.env` 不进镜像 |

**实践建议：** 优化 `blog-api/Dockerfile`，镜像体积减小；编写 `docker-compose` 一键起 MySQL + Redis + RabbitMQ + API。

#### 5.3 Kubernetes（K8s）

| 概念 | 说明 |
|------|------|
| 与 Docker 关系 | Docker 运行容器；K8s 在集群中调度、扩缩容、自愈 |
| 核心对象 | Pod、Deployment、Service、Ingress、ConfigMap、Secret |
| .NET 部署 | 容器化 API → Deployment；Service 暴露；Ingress 替代部分 Nginx 职责 |

**实践建议：**

1. 本地用 minikube 或 Docker Desktop 内置 K8s。
2. 把 `blog-api` 部署为 Deployment（2 副本）+ Service。
3. 理解滚动更新与 `kubectl rollout undo`。

**达标标准：** 能说出 Pod / Deployment / Service / Ingress 各解决什么问题；知道何时用 Compose、何时上 K8s。

**推荐资料：**

- GitHub Actions 官方文档
- 《Docker 实战》
- Kubernetes 官方教程（中文）或 B 站「K8s 入门」

---

### 阶段 6：微服务架构设计思想（压轴）

> **目标：** 知道何时拆、怎么拆、拆完带来什么代价——不是「为了微服务而微服务」。

#### 6.1 核心思想

| 主题 | 内容 |
|------|------|
| 拆分依据 | **业务能力 / 限界上下文**，不是按 Controller 数量 |
| 通信 | 同步 REST/gRPC（查询、实时）；异步 MQ（事件、削峰） |
| 数据 | 一服务一库；禁止跨库 JOIN；最终一致 |
| 基础设施 | 注册发现、配置中心、API 网关、链路追踪 |
| 代价 | 分布式事务、运维复杂度、调试难度上升 |

#### 6.2 与「多机部署」的区别

| 对比 | 多机 + 负载均衡 | 微服务 |
|------|-----------------|--------|
| 代码 | 同一套 `blog-api` 多实例 | 用户服务、留言服务、搜索服务 **不同代码库** |
| 扩展 | 整体扩容 | 按服务独立扩容（搜索服务多副本） |
| 数据 | 通常共用一个库 | 各服务独立数据库 |

#### 6.3 演进路径（与你项目对应）

```
单体 blog-api（当前）
    ↓ 流量增大
单体 + Nginx 负载均衡 + Redis + 主从库
    ↓ 团队 / 业务变大
按域拆分：MessageService + SearchService（ES）+ NotifyService（MQ）
    ↓ 实例众多
K8s 编排 + CI/CD + 可观测性平台
```

**实践建议：**

1. **纸上设计：** 把博客拆成「留言服务 + 搜索服务 + 通知服务」，画调用图与数据归属。
2. **不必真拆：** 当前规模单体更合适；理解思想即可。
3. 读马丁·福勒《Microservices》系列文章；对照公司架构图标注各组件。

**达标标准：** 面试能回答「什么时候不该用微服务」；能结合 RabbitMQ、ES、Redis 说明一个完整分布式场景。

**推荐资料：**

- martinfowler.com «Microservices»
- 《微服务设计》Sam Newman
- .NET 微服务参考架构（Microsoft eShopOnContainers，选读）

---

## 四、技术点速查表

| 技术 | 解决什么问题 | 建议阶段 | 博客项目可实践 |
|------|--------------|----------|----------------|
| IOC / DI | 解耦、可测试 | 1 | Repository 注入 |
| AOP | 日志、鉴权、异常横切 | 1 | 全局异常中间件 |
| DDD + 仓储 | 清晰分层、领域建模 | 1 | Message 聚合 + Repository |
| Nginx 反代 | 统一入口、隐藏后端 | 0 ✅ | 已部署 |
| Nginx 负载均衡 | 多 API 实例分摊流量 | 2 | 本地双实例实验 |
| Redis | 缓存、锁、限流、Session | 2 | 留言列表缓存、防刷 |
| 主从数据库 | 读扩展、备份 | 2 | 读文档 + 画架构图 |
| 高并发 / 高可用 | 扛流量、故障恢复 | 3 | 压测 + Polly |
| 幂等 | 重复请求不重复处理 | 3 | Request-Id 去重 |
| RabbitMQ | 异步、解耦、削峰 | 4 | 留言后异步通知 |
| ElasticSearch | 全文搜索、日志检索 | 4 | 留言关键词搜索 |
| CI/CD | 自动构建测试部署 | 5 ✅ CI | GitHub Actions（见 5.1） |
| Docker | 环境一致、交付 | 0 ✅ / 5 进阶 | 多阶段 Dockerfile |
| K8s | 集群编排、弹性伸缩 | 5 | minikube 部署 API |
| 微服务思想 | 大系统拆分与治理 | 6 | 纸上拆分设计 |

---

## 五、学习建议（针对 ASP.NET 工程师）

### 5.1 学习方法

1. **以项目驱动，不要纯看视频**  
   每学一个技术，就在 `blog-api` 或本地实验环境加一个小功能；学 Redis 就加缓存，学 MQ 就加异步通知。

2. **先跑通再优化**  
   Docker 单容器 → Compose 多服务 → K8s 两副本；不要第一步就追求 HA 集群。

3. **画图 + 讲给别人听**  
   每完成一个阶段，画一张架构图并用 5 分钟讲清数据流；费曼学习法对架构知识特别有效。

4. **对照 TECH.md 复盘**  
   [TECH.md](./TECH.md) 里已有 Nginx、LB、主从、MQ、K8s 概念归纳；学完每个点后回去标记「已实践」。

5. **记录踩坑**  
   建一个 `notes/` 或私人仓库，记录：CORS、502、MQ 重复消费、ES 同步延迟等——面试时都是素材。

### 5.2 常见误区

| 误区 | 正确做法 |
|------|----------|
| 先学微服务再学 Redis/MQ | 微服务是「封顶」；底层组件要先会用 |
| 把 DDD 做成过度抽象 | 博客规模：Service + Repository 足够 |
| 忽略幂等只关心「能跑」 | MQ、支付、回调场景幂等是必考题 |
| 本地从不压测 | 用 k6/JMeter 压留言接口，建立性能直觉 |
| K8s 代替 Docker 基础 | Compose 熟练后再学 K8s，否则排查困难 |
| ES 替代 MySQL | MySQL 是真相源；ES 是搜索视图 |

### 5.3 时间分配建议（每周 10 小时示例）

| 活动 | 时间 |
|------|------|
| 官方文档 / 书 | 3h |
| 动手实验（博客项目） | 5h |
| 总结笔记 / 画图 | 1h |
| 阅读一篇架构案例 | 1h |

### 5.4 检验清单（中级 → 中高级）

- [ ] 能独立搭建：Nginx + 双 API 实例 + Redis + MySQL 主从（概念或实验环境）
- [ ] 能实现：留言 → RabbitMQ → 消费者，且消费幂等
- [ ] 能实现：留言同步 ES 并提供搜索 API
- [x] 能配置：GitHub Actions 自动 `dotnet test` + Docker build（见 [5.1](#51-cicddevops)）
- [ ] 能在 minikube 部署 API Deployment 并滚动更新
- [ ] 能白板讲解：微服务拆分边界、一服务一库、最终一致
- [ ] 能排查：502、缓存不一致、MQ 堆积、ES 与 MySQL 数据延迟

---

## 六、推荐学习资源

### 官方文档（优先）

- [ASP.NET Core 文档](https://learn.microsoft.com/zh-cn/aspnet/core/)
- [Entity Framework Core](https://learn.microsoft.com/zh-cn/ef/core/)
- [Redis 文档](https://redis.io/docs/)
- [RabbitMQ Tutorials](https://www.rabbitmq.com/tutorials)
- [Elastic 指南](https://www.elastic.co/guide/index.html)
- [Docker 文档](https://docs.docker.com/)
- [Kubernetes 文档](https://kubernetes.io/zh-cn/docs/home/)

### 书籍 / 课程（选读）

| 方向 | 资源 |
|------|------|
| DDD | 《领域驱动设计》Eric Evans；《实现领域驱动设计》Vaughn Vernon |
| 分布式 | 《微服务设计》Sam Newman；《数据密集型应用系统设计》DDIA（进阶） |
| Redis | 《Redis 设计与实现》黄健宏 |
| 消息队列 | 《RabbitMQ 实战》 |
| .NET 微服务 | eShopOnContainers 示例仓库 |

### 与本仓库文档联动

| 文档 | 用途 |
|------|------|
| [README.md](./README.md) | 部署流程、全链路理解 |
| [TECH.md](./TECH.md) | 架构概念、阶段 0～5 对照 |
| [DOCKER.md](./DOCKER.md) | Docker 本地实践 |
| [DEPLOY-CLOUD.md](./DEPLOY-CLOUD.md) | 云服务器与 Nginx |
| [`.github/workflows/ci.yml`](./.github/workflows/ci.yml) | CI 流水线（测试 + Docker build） |

---

## 七、Quick Reference（复习 / 面试用）

```
IOC/DI     → 依赖由容器注入，便于测试与替换实现
AOP        → 日志/鉴权/异常与业务分离（Middleware、Filter）
DDD+仓储   → 领域建模 + IRepository 隐藏持久化细节

Nginx 反代 → 统一入口，/api 转到内网 API
负载均衡   → upstream 多实例轮询，应用层扩展
Redis      → 缓存、锁、限流；注意穿透/击穿/雪崩
主从库     → 主写从读，注意复制延迟

高并发     → 无状态、异步、池化、削峰、限流
高可用     → 多副本、健康检查、熔断降级、故障转移
幂等       → 同一业务键只处理一次（HTTP/MQ/回调必考）

RabbitMQ   → 异步解耦削峰；持久化 + ACK + 幂等消费
ES         → 全文检索；MySQL 同步过来，不做唯一真相源

CI/CD      → 提交即构建测试；合格产物自动部署
Docker     → 镜像交付；Compose 本地，K8s 集群
K8s        → Pod/Deployment/Service/Ingress 编排与扩缩容
微服务     → 按业务能力拆分，一服务一库，最后再学
```

---

## 八、附录：Redis 特性与面试题（结合 blog-api）

> 与阶段 2.2 配套；代码实践见 `blog-api/Services/MessageListCacheService.cs`。

### 8.1 Redis 是什么？

- **基于内存** 的 Key-Value 数据库，读写延迟远低于 MySQL（微秒～亚毫秒级）。
- 支持 **持久化**（RDB 快照、AOF 日志），重启可恢复数据（视配置而定）。
- 单线程处理命令（6.0+ 多线程主要做 IO），避免锁竞争，适合高 QPS 简单操作。
- 与 MySQL 关系：**MySQL 是业务真相源；Redis 是加速层**，Redis 数据应能重建或接受短暂不一致。

### 8.2 五种基本数据结构

| 类型 | 典型命令 | 用途 |
|------|----------|------|
| **String** | `SET` / `GET` / `INCR` | 缓存 JSON、计数器、分布式锁（`SET NX EX`） |
| **Hash** | `HSET` / `HGETALL` | 存对象字段（用户信息） |
| **List** | `LPUSH` / `RPOP` | 简单队列、最新 N 条记录 |
| **Set** | `SADD` / `SISMEMBER` | 去重、共同好友 |
| **Sorted Set** | `ZADD` / `ZRANGE` | 排行榜、延迟队列 |

本项目的留言列表缓存用的是 **String**（整段 JSON 序列化后存入），通过 `IDistributedCache` 抽象访问。

### 8.3 常见使用场景

| 场景 | 说明 | 博客项目 |
|------|------|----------|
| **缓存** | 热点读走 Redis，减轻 DB | ✅ `GET /api/messages` 列表，10 分钟 TTL |
| **Session** | 多 API 实例共享登录态 | 当前用 `X-Admin-Key` 无状态，暂不需要 |
| **限流** | `INCR` + 过期时间，防刷 | 计划：同 Email 60 秒内只能留言一次 |
| **分布式锁** | `SET key NX EX` | 定时任务、防重复处理 |
| **消息队列** | List / Stream | 轻量异步；量大用 RabbitMQ |

### 8.4 三种缓存模式（面试必考）

| 模式 | 流程 | 本项目 |
|------|------|--------|
| **Cache-Aside（旁路）** | 读：先缓存后 DB；写：先 DB 再删/失效缓存 | ✅ 留言列表 |
| **Read-Through** | 由缓存组件负责加载 DB | 未用 |
| **Write-Through** | 写同时更新缓存和 DB | 未用 |

**本仓库 Cache-Aside 流程：**

```
GET /api/messages
  → TryGetAsync（Redis）
  → 命中：直接返回
  → 未命中：查 MySQL → SetAsync（TTL 10min）→ 返回

POST / DELETE 留言
  → 写 MySQL
  → InvalidateAsync（版本号 +1，旧分页 key 不再被读取）
```

**为何用「版本号」而不是删所有 key？**  
列表有多页（`page`、`pageSize` 组合多），逐个 `DEL` 麻烦；递增 `messages:list:version` 后，新读使用新 key 前缀，旧 key 最多再占内存 10 分钟由 TTL 清理。

### 8.5 缓存三大问题与对策

| 问题 | 含义 | 常见对策 |
|------|------|----------|
| **缓存穿透** | 查不存在的数据，缓存没有，每次都打 DB | 空结果也缓存短 TTL；布隆过滤器 |
| **缓存击穿** | 热点 key 过期瞬间，大量请求同时打 DB | 互斥锁重建；热点 key 永不过期 + 异步刷新 |
| **缓存雪崩** | 大量 key 同时过期，DB 压力暴增 | TTL 加随机值；集群限流；多级缓存 |

留言列表若被频繁访问，可注意 **击穿**（10 分钟到期瞬间）；当前流量小，版本号 + TTL 已够用。

### 8.6 持久化与高可用（面试常问）

| 机制 | 说明 |
|------|------|
| **RDB** | 定时快照，恢复快，可能丢最后一次快照后的数据 |
| **AOF** | 记录每条写命令，数据更完整，文件更大 |
| **主从复制** | 从节点同步主节点，读写分离、备份 |
| **哨兵 Sentinel** | 自动故障转移，选新主节点 |
| **Cluster 集群** | 数据分片，水平扩展 |

生产环境：小项目单机 Redis 即可；流量大再上哨兵/集群。

### 8.7 与 MySQL、多实例的关系

```
浏览器 → Nginx → blog-api-1 / blog-api-2
                      ↓
                 Redis（共享缓存）
                      ↓
                 MySQL（唯一真相源）
```

- 两个 API 实例必须连 **同一个 Redis**，否则各自内存缓存不一致。
- 配置：`ConnectionStrings:Redis` = `redis:6379`（Compose 服务名）。

### 8.8 面试高频题速查

| 问题 | 参考答案要点 |
|------|----------------|
| Redis 为什么快？ | 内存、单线程无锁竞争、IO 多路复用、高效数据结构 |
| Redis 和 Memcached 区别？ | Redis 数据结构丰富、可持久化；Memcached 更简单、纯内存 |
| 如何保证缓存与 DB 一致？ | Cache-Aside + 写后失效/更新；接受最终一致；关键读可走主库 |
| 分布式锁如何实现？ | `SET lock_key unique NX EX 30`；释放前校验 value；Redisson 等库 |
| 缓存穿透/击穿/雪崩？ | 见 8.5 表 |
| Redis 单线程为何还快？ | 内存操作极快；非 CPU 密集；6.0 网络 IO 可多线程 |
| 过期 key 怎么删除？ | 惰性删除（访问时）+ 定期抽样删除 |
| 什么是热 key？ | 访问极度集中的 key；解决：本地缓存、拆分、Redis Cluster |
| 留言列表为何适合缓存？ | 读多写少；管理员列表可接受最多 10 分钟延迟（可配置） |
| 留言后为何要 Invalidate？ | 否则 10 分钟内列表仍显示旧数据 |

### 8.9 本地验证缓存是否生效

```bash
# 1. 启动栈（含 Redis）
docker compose up -d --build

# 2. 第一次拉列表（会写缓存）
curl -s -H "X-Admin-Key: dev-admin-key-change-in-production" \
  "http://localhost:5059/api/messages?page=1&pageSize=5"

# 3. 看 Redis 里是否有 key
docker exec -it blog-redis redis-cli KEYS 'blog:*'

# 4. 提交新留言后再拉列表，版本号应变化，旧 list key 不再使用
```

日志中可见 `Message list cache hit`（命中）或 `Message list cached`（回填）。

### 8.10 配置项

| 配置 | 位置 | 默认 |
|------|------|------|
| `ConnectionStrings:Redis` | `appsettings*.json` / Docker 环境变量 | `127.0.0.1:6379` |
| `Redis:MessageListCacheMinutes` | `appsettings*.json` | `10` |

修改 TTL 后重启 API 即可，无需改代码。

---

*文档版本：2026-06-04 · 基于 ASP.NET 工程师技术清单与墨言博客项目整理（含 Redis 留言列表缓存实践）*
